# AIPO Backend Agent Guide

## 1. Project Summary
AIPO는 **RAG 기반 공시문서 분석 및 IPO 투자 의사결정 지원 AI 챗봇** 서비스다.
초보 투자자도 공시문서와 IPO 데이터를 쉽게 이해하고, 근거 기반 답변과 개인화된 투자 보조 정보를 받을 수 있도록 돕는 것을 목표로 한다.

## 2. Product Scope
### 2.1 User-facing Scope
- 사용자 인증: 로그인, 로그아웃, 회원가입, 회원탈퇴
- 투자 성향 관리: 검사, 스킵, 재검사
- 공모주 탐색: 목록 조회, 검색, 정렬, 인기 종목 노출
- 종목 상세: 기본 정보, 매력지수, 공모 정보, 기업 정보, 경쟁률, 수요예측 결과
- 관심 종목: 등록/해제, 조회, 삭제, 정렬, 알림 설정
- 캘린더: 월별 일정, 일정 유형 필터, 종목 검색, 날짜별 기업 목록, 상세 이동
- 챗봇: 새 채팅, 질문/답변, 추천 질문, 답변 평가, 채팅 기록 조회/삭제/고정

### 2.2 Admin-facing Scope
- 관리자 인증: 로그인, 로그아웃
- 서비스 현황 통계: 활성 사용자, RAG 종목 수, 챗봇 만족도, API 사용량
- 공모주 조회 통계: 조회 급증 종목, 관심 종목 Top 10
- 외부 데이터 API 관리: 상태 조회, 크롤링 모니터링, 수동 호출, 업데이트 주기 설정
- RAG 데이터베이스 관리: PDF 업로드, 텍스트 추출, 임베딩 생성, 벡터 DB 저장/관리
- 매력지수 관리: 가중치 조절
- 챗봇 로그 관리: 대화 로그, 첫 질문 수집/분류, 평가 기반 질문 수집
- 참고 문서 관리: 등록, 수정/삭제, 버전 관리
- 사용자 관리: 목록 조회, 검색, 삭제/비활성화

### 2.3 AI / RAG Scope
- 공시 PDF 파싱
- 텍스트 정제 및 정규화
- 한국어 형태소 분석
- 텍스트 임베딩 생성
- 벡터 DB 검색
- 근거 기반 답변 생성
- 핵심 투자 지표 추출
- 리스크 요인 분류
- 투자 매력도 산출 및 랭킹
- 프롬프트 템플릿 관리
- 성능 모니터링 및 로깅

## 3. Architecture Intent
- Backend는 사용자 앱, 관리자 웹, AI 엔진, 정형 DB, Vector DB가 협업하는 구조를 전제로 한다.
- 사용자용 API와 관리자용 API는 논리적으로 분리한다.
- 챗봇 응답은 AI/RAG 파이프라인과 연동되는 구조를 유지한다.
- 정형 데이터와 벡터 검색 데이터를 혼합 저장하지 않는다.
- 관리자 기능은 운영, 통계, 데이터 품질 관리 목적에 맞게 설계한다.

## 4. Coding Rules
- 기존 패키지 구조와 네이밍 규칙을 최대한 유지한다.
- 먼저 계획을 제시하고, 승인 없이 큰 범위 수정은 하지 않는다.
- 한 번에 하나의 기능 단위만 구현한다.
- 기존 공통 응답 형식과 예외 처리 방식을 우선 따른다.
- 문서에 없는 필드나 기능을 임의로 추가하지 않는다.
- 인증/인가 흐름은 임의로 바꾸지 않는다.
- 종목 상세 진입에 필요한 식별자(`ipoId` 또는 동일 역할 키)는 일관되게 유지한다.
- 외부 API, 배치, 크롤링, RAG 파이프라인은 사용자 요청 처리 코드와 적절히 분리한다.

## 5. API Design Rules
- 모든 API는 역할별 책임이 명확해야 한다.
- 사용자 API와 관리자 API를 혼용하지 않는다.
- 읽기 API와 변경 API를 명확히 구분한다.
- 검색/정렬/필터는 request parameter 또는 request DTO에서 일관되게 다룬다.
- 상세 조회 응답은 화면 구성 단위에 맞추되, 백엔드 내부 모델을 그대로 노출하지 않는다.
- 챗봇 API는 세션/질문/답변/피드백 흐름을 구분해서 설계한다.

## 6. Data Rules
- 사용자, 종목, 관심 종목, 일정, 대화 기록, 통계, 로그는 정형 DB에 저장한다.
- 공시 원문, 청크, 임베딩, 검색용 메타데이터는 Vector DB 또는 문서 저장소와 연계해 관리한다.
- 매력지수는 사용자 투자 성향과 종목 지표를 함께 고려할 수 있도록 설계한다.
- 관리자 통계는 원본 로그와 집계 데이터를 분리해 다루는 방향을 우선 고려한다.

## 7. Testing / Validation Rules
- 기능 추가 시 최소한 다음을 함께 검토한다:
  1. 요청/응답 스키마 일관성
  2. 인증/인가 영향 범위
  3. 예외 처리 메시지
  4. 목록/상세/검색/정렬 동작
  5. 관리자 기능과 사용자 기능 간 권한 분리
- 문서 기반 기능은 가능한 경우 설계 문서의 용어와 동일하게 유지한다.
- RAG/AI 관련 기능은 “근거 기반 응답”과 “모르는 것은 모른다고 답하는 정책”을 훼손하지 않는다.

## 8. Preferred Work Order
1. 관련 문서 범위 확인
2. 현재 코드 구조 파악
3. 변경 대상 파일 목록 제시
4. DTO → Service → Controller → Repository 순서로 작은 단위 구현
5. 테스트/검증 포인트 정리
6. 변경 위험 요소 요약

## 9. Do Not
- 문서에 없는 화면/기능을 임의로 확장하지 말 것
- 기존 인증 토큰 흐름을 근거 없이 바꾸지 말 것
- 관리자 기능을 사용자 API에 섞지 말 것
- 벡터 검색 로직을 일반 CRUD 로직과 무분별하게 결합하지 말 것
- 매력지수 산정 로직을 하드코딩된 임시 값으로 대체하지 말 것
