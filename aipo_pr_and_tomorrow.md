# AIPO PR And Tomorrow

이 문서는 AIPO 백엔드 작업 기준 문서다.
오늘 완료한 작업, 현재 상태, 내일 할 일, 주의할 점을 빠르게 확인하기 위한 용도로 유지한다.

- 다음 작업을 시작하기 전에 먼저 이 문서를 확인한다.
- 문서 내용과 현재 코드 상태가 다르면 차이를 먼저 확인하고 갱신한다.
- 이 문서는 작업 기준 문서이며, 개인 메모와는 구분해서 관리한다.

## 오늘 완료한 작업

### 1. 내부 API 구현 정리
- 회원가입 API 정리
- 로그인 / 로그아웃 / 토큰 재발급 / 인증 예외 처리 정리
- 공모주 상세 조회 API 구현 정리
- 관심종목 조회 / 등록 / 삭제 API 구현 정리
- 캘린더 월별 일정 조회 API 구현 정리
- 채팅 세션 / 메시지 / 피드백 / 추천 질문 API 구현 정리
- 투자성향 검사 / 스킵 / 재검사 / 결과 조회 API 구현 정리
- 마이페이지 조회 / 알림 설정 / 내 정보 / 비밀번호 변경 / 회원탈퇴 API 구현 정리

### 2. 내부 API 안정화
- 로그인 실패가 500이 아니라 401로 내려가도록 예외 처리 보완
- 로그아웃 시 잘못된 토큰 / 누락된 헤더가 500이 아니라 적절한 인증 오류로 내려가도록 정리
- 미검사 사용자 마이페이지 조회 시 `NOT_TESTED` fallback 응답이 정상 반환되도록 보완
- Swagger 기준 공개 API / 인증 API / 사용자 API 동작 점검

### 3. 캘린더 / 채팅 / 투자성향 / 마이페이지 반영
- `CalendarController`, `CalendarControllerTest` 구현
- 채팅 저장 구조 및 API 구현 반영
- 투자성향 검사 도메인 및 API 구현 반영
- 마이페이지 설정 섹션 중심 API 구현 반영

### 4. 외부 연동 1차 프로토타입 제거
- KIND 외부 연동 client / dto / sync / facade / scheduler / admin sync controller 제거
- external sync 관련 설정값 제거
- external sync 관련 테스트 제거
- external sync 때문에 임시 완화했던 보안 설정 정리

### 5. 테스트 및 브랜치 정리
- 내부 API 회귀 테스트 통과
  - auth
  - ipo 상세
  - favorite
  - calendar
  - chat
  - investmentprofile
  - mypage
- 현재 작업 브랜치 원격 push 완료

## 현재 상태

- 내부 API 중심 기능은 일단 구현 완료 상태다.
- 현재 기준으로 사용자 기능 백엔드 뼈대는 모두 연결 가능한 수준까지 정리되었다.
- 외부 API 연동은 이번 단계에서 보류 상태다.
- 외부 연동 코드는 제거된 상태다.
- 다음 단계의 중심은 외부 연동이 아니라 프론트 연동과 보고서 작성이다.

## 내일 할 일

### 1. 프론트와 백엔드 연동
- Swagger 기준으로 필요한 request / response를 다시 점검
- 프론트 화면과 API 응답 구조를 맞추기
- 실제 화면 흐름 기준으로 누락된 필드나 응답 형태가 없는지 확인
- 프론트 연동 중 발견되는 API 수정사항이 있으면 목록화해서 정리

### 2. 보고서 작성
- 오늘까지 구현한 기능 범위 정리
- 테스트 결과 정리
- 구현 결과 정리
- API 목록 정리
- 주요 화면 흐름 정리

### 3. 문서 보완
- Swagger 기준 호출 순서와 테스트 방법이 필요한 API는 다시 정리
- 프론트 연동 과정에서 바뀌는 request / response가 있으면 문서 반영

## 주의할 점

- 다음 단계의 우선순위는 외부 연동이 아니라 프론트 연동과 보고서 작성이다.
- 외부 연동 코드는 현재 제거된 상태이므로, 다시 시작할 때는 별도 작업으로 다시 설계하고 붙여야 한다.
- `AGENTS.md`, `docs/`, 개인 메모 파일은 이번 구현 커밋 범위와 분리해서 관리한다.
- 로컬 전용 설정 파일은 커밋 대상과 분리해서 유지한다.
- 프론트 연동 중 발견되는 API 수정은 최소 수정 기준으로 정리하고, 화면 기준 영향 범위를 함께 기록한다.

